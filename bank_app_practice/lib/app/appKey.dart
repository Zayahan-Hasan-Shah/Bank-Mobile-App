//Keys

const tokenKey = 'tokenKey';
const userIdKey = 'userId';
const loginModelKey = 'loginModel';  

const userAuthKey = 'authKey';
const rememberMeSelectedKey = 'rememberMe';

///this is used to store the product_date_change value from config table. If its gets changed that means products is
///updated and will store in local storage.
const userConfigValueKey = 'userConfigValue';
